import React from "react";
import Spinny from "../components/Spinner/Spinner";

const Fallback = () => {
  return (
    <div className="FallbackWrapper">
      <Spinny />
    </div>
  );
};

export default Fallback;
